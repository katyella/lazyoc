package constants

// API endpoints and paths
const (
	// OpenShiftAPIPath is the base path for OpenShift API
	OpenShiftAPIPath = "/oapi"

	// MetricsEndpoint is the endpoint for metrics
	MetricsEndpoint = "/metrics"

	// HealthEndpoint is the endpoint for health checks
	HealthEndpoint = "/health"

	// ReadyEndpoint is the endpoint for readiness checks
	ReadyEndpoint = "/ready"
)
